#!/usr/bin/env bash

declare -r install_path="/var/sandbox/asakeron/darksouls/prefix/drive_c/Program\ Files\ \(x86\)/DARK\ SOULS\ REMASTERED/"
declare -r prefix="/var/sandbox/asakeron/darksouls/prefix"

firejail --profile=darksouls -- \
  /bin/sh -c "cd $install_path && WINEPREFIX=$prefix exec wine DarkSoulsRemastered.exe"
  
#  /bin/sh -c "cd $install_path && WINEPREFIX=$prefix exec wine control joy.cpl"
  
#  bash -c 'realpath .' # \
